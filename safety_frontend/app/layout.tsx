import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import { Providers } from "./providers"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  // <CHANGE> Updated metadata to reflect AI Content Studio branding
  title: "AI Content Studio - Generate Smarter, Ship Faster",
  description: "The all-in-one AI-powered platform for creating blogs, social media posts, and custom images. Generate professional content in seconds.",
  generator: "v0.app",
  icons: {
    icon: [
      {
        url: "/icon-light-32x32.png",
        media: "(prefers-color-scheme: light)",
      },
      {
        url: "/icon-dark-32x32.png",
        media: "(prefers-color-scheme: dark)",
      },
      {
        url: "/icon.svg",
        type: "image/svg+xml",
      },
    ],
    apple: "/apple-icon.png",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    // <CHANGE> Added dark class by default for modern dark-first design
    <html lang="en" className="dark">
      <body className={`font-sans antialiased relative`}>
        <Providers>{children}</Providers>
        <Analytics />
      </body>
    </html>
  )
}
